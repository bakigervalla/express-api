# Cars Web Solutions API

1. Install dependencies

`npm install`

2. Setup local environment

`npm run setup.staging`

3. Start dev server

`npm start.dev` (also builds docs)

4. Build docs

`npm run build.docs`
